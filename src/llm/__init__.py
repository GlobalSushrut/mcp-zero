# MCP-ZERO LLM Layer — thin Python orchestration for inference only.
# All cognitive compute (vectors, reasoning, entropy, symbols) lives in Rust.
# Python handles: LLM inference, embedding generation, prompt construction.
